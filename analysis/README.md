# analysis/

The analysis code and notebook live here.

Plan:
1. `fetch_data.py` — download the total-return series and save to `../data/`.
2. `build_portfolios.py` — construct the risk-graded portfolios, rebalanced annually.
3. `analyse.ipynb` — compute the risk measures, run the three tests, and produce the charts in `../figures/`.

Not written yet. This directory is scaffolding for the steps described in the top-level README.
